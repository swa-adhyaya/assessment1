version: 0.2

phases:
  install:
    commands:
      - echo "Install phase"

  build:
    commands:
      - echo "Build phase"

  post_build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - '**/*'
